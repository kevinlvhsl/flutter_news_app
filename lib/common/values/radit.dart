import 'package:flutter/rendering.dart';

class Radit {
  static const BorderRadiusGeometry radius6px =
      BorderRadius.all(Radius.circular(6));
  static const BorderRadiusGeometry radius12px =
      BorderRadius.all(Radius.circular(12));
}
